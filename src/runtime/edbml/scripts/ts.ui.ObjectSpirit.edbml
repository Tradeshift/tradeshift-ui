<!DOCTYPE edbml>
<script>
	<?param name="object" type="ts.ui.ObjectModel"?>
	var classconfig = this.css.name();
	var contentonly = true;
	if(object) {
		out.html += object.render(contentonly, classconfig);
	} else {
		// unless the {ts.ui.ObjectSpirit} provides a mock model, 
		// the preloading layout must be implemented with CSS only.
	}
</script>
